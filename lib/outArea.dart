import 'package:flutter/material.dart';

class OutArea extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Column(
        mainAxisAlignment: MainAxisAlignment.center,
        children: [
          Text('지원하는 지역 밖입니다.'),
        ],
      ),
    );
  }
}